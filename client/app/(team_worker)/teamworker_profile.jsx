import { View, Text } from "react-native";
import React from "react";

const TeamworkerProfile = () => {
	return (
		<View>
			<Text>TeamworkerProfile</Text>
		</View>
	);
};

export default TeamworkerProfile;
